# Curation-time dependencies — NOT runtime deps of mat.
#
# Install with:
#   pip install -r scripts/requirements-curation.txt
#   python -m playwright install chromium   # one-time, only if rendering previews
#
# Used by:
#   scripts/enrich_from_wikidata.py  — Wikidata SPARQL cross-checks
#   scripts/enrich_vis.py            — mat-vis index tag matching (uses pymat.vis)
#   scripts/generate_previews.py     — headless Three.js catalog previews

requests>=2.28
playwright>=1.40
# tomlkit: roundtrip-safe TOML for scripts/_curation.writeback. Preserves
# comments and formatting when enrichers patch values into data TOMLs.
tomlkit>=0.12
# pyyaml: refractiveindex.info ships its database as YAML 1.1 files;
# used by scripts/enrich_from_refractiveindex.py (#164). NOT a runtime dep.
pyyaml>=6.0
